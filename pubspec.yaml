name: better_quiz_game
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  youtube_player_iframe: ^6.0.2
  google_fonts: ^8.2.0
  dropdown_search: ^7.0.0
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/db/chunithm video.json
    - assets/db/chunithm sheets.json